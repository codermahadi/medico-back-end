export enum activeStatus {
    ONLINE = 'online',
    OFFLINE = 'offline',
    BUSY = 'busy'
}

export enum Channel {
    WEB = 'web',
    IOS = 'ios',
    ANDROID = 'android'
}

export enum SingUpGateway {
    GOOGLE = 'google',
    FACEBOOK = 'facebook',
    ADMIN_PANEL = 'adminPanel',
    VAS = 'vas'
}

export enum Bucket {
    DOCTORS = 'doctors',
    PATIENTS = 'patients'
}
